import styles from './RecordMatch.module.css';

export default function RecordMatch(props) {
    return (
        <div className={styles.container}>
            <p>RecordMatch component</p>
        </div>
    )
};